class Place {
  constructor(id, title, imageUri, address, lng, lat) {
    (this.id = id),
      (this.title = title),
      (this.imageUri = imageUri),
      (this.address = address),
      (this.lng = lng),
      (this.lat = lat);
  }
}

export default Place;
